TO RUN:

	Add this directory to your %path to be able to call dao from anywhere.
	Use cmd and cd to this directory.
	
	In the command line, use
		> dao <filename>.dao
		
	To compile to <filename>.wuwei, which is the code format.
	
	In the command line, use
		> dao <filename>.wuwei
		
	To execute a file.